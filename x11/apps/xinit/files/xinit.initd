#!/sbin/openrc-run

name="xinit"
description="X11 autologin service"

start(){
    /bin/busybox su - $USER -c "exec /usr/bin/startx" &
    exit 0
}

depend()
{
        need dbus
        provide gui display-manager getty
}
