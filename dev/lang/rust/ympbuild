#!/usr/bin/env bash
name='rust'
release='2'
version='1.74.0'
url='https://www.rust-lang.org'
description='Systems programming language from Mozilla'
email='aliriza.keskin@pardus.org.tr'
maintainer='aliriza'
license=('GPLv3')
source=("https://static.rust-lang.org/dist/rustc-$version-src.tar.gz"
        'config.toml')
depends=(curl gcc libssh llvm)
makedepends=(cmake ninja perl python)
md5sums=('96325c20a8fc75fa062647a84003e5d3'
         'SKIP')
group=(dev.lang)
uses=()
arch=('x86_64')
# disable strip
dontstrip=1

cd rustc-$version-src

setup () {
    install ../config.toml ./config.toml
}

build () {
    python3 ./x.py build -j `nproc`
}

package () {
    python3 ./x.py install -j `nproc`
}

